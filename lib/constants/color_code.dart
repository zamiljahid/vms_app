import 'package:flutter/material.dart';

class ColorCode {
  // ===== RED THEME =====
  static const redDarkBackground = Color(0xFF2B1B1B);     // Deep maroon background
  static const redCardColor = Color(0xFF4A2C2C);          // Dark crimson card
  static const redPrimaryAccent = Color(0xFFEB4D4B);      // Flamingo red
  static const redLightHighlight = Color(0xFFFFC1C1);     // Soft pink highlight

  // ===== BLUE THEME =====
  static const blueDarkBackground = Color(0xFF0D1B2A);    // Deep navy blue background
  static const blueCardColor = Color(0xFF1B263B);          // Slightly lighter card
  static const bluePrimaryAccent = Color(0xFF1E90FF);      // Dodger Blue
  static const blueLightHighlight = Color(0xFF81CFFF);     // Light blue highlight

  // ===== GREEN THEME =====
  static const greenDarkBackground = Color(0xFF1B2F2A);   // Deep forest green background
  static const greenCardColor = Color(0xFF264D3B);         // Dark muted green card
  static const greenPrimaryAccent = Color(0xFF2ECC71);     // Emerald green
  static const greenLightHighlight = Color(0xFFA8E6CF);    // Light mint green

  // ===== PURPLE / VIOLET THEME =====
  static const purpleDarkBackground = Color(0xFF251C36);  // Dark purple background
  static const purpleCardColor = Color(0xFF3C2A5D);        // Muted violet card
  static const purplePrimaryAccent = Color(0xFF8E44AD);    // Rich purple accent
  static const purpleLightHighlight = Color(0xFFD7BDE2);   // Soft lilac highlight

  // ===== ORANGE THEME =====
  static const orangeDarkBackground = Color(0xFF1C1C1E);   // Dark gray background for orange theme
  static const orangeCardColor = Color(0xFF2C2C2E);        // Card color dark gray
  static const orangePrimaryAccent = Color(0xFFFF9500);    // Bright orange accent
  static const orangeLightHighlight = Color(0xFFFFB84D);   // Light orange highlight

  // ===== AMBER / WARM THEME =====
  static const amberDarkBackground = Color(0xFF2B1F0F);    // Dark amber-brown background
  static const amberCardColor = Color(0xFF5A3D1E);         // Burnt orange card
  static const amberPrimaryAccent = Color(0xFFFFA726);     // Warm amber-orange
  static const amberLightHighlight = Color(0xFFFFE0B2);    // Light amber highlight

  // ----- Common colors (can be shared across themes) -----
  static const white = Color(0xFFFFFFFF);
  static const semiWhite = Color(0xFFF5F5F5);
  static const black = Color(0xFF000000);

  static const primaryText = Color(0xFFFFFFFF);
  static const secondaryText = Color(0xFFBBBBBB);

  static const transparentWhiteHex8 = Color(0x88FFFFFF);
  static const blackSemiTransparent = Color(0x9A000000);

  // Shadows (consistent usage)
  static const shadowLight = Color(0xFF3C3C3E);
  static const shadowDark = Color(0xFF0D0D0E);
}
